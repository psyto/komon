[package]
name = "problem-registry"
version = "0.1.0"
description = "Komon Problem Registry - Register civic problems on Solana"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "problem_registry"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.30.0"
anchor-spl = "0.30.0"
